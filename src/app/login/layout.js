export const metadata = {
  title: 'تسجيل الدخول | مُلم',
  description: 'سجّل دخولك إلى منصة مُلم',
  robots: { index: false, follow: false },
};

export default function Layout({ children }) {
  return children;
}
